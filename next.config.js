/** @type {import('next').NextConfig} */
const nextConfig = {
  // Fonts loaded from Google — allow external images if needed later
  images: {
    remotePatterns: [],
  },
}

module.exports = nextConfig
